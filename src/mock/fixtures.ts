/**
 * Sample data slot. Intentionally EMPTY — Alamara starts with a clean vault and
 * fills up only from what the user scans or imports. (Kept as a module so any
 * historical import path still resolves.)
 */

import type { Document, Ticket } from '@/types/models';

export const DOCUMENTS: Document[] = [];
export const TICKETS: Ticket[] = [];
