# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Alamara engineering

This is the Alamara local-first document vault (Expo SDK 56 / RN 0.85, source under `src/`).
Before writing code, read **`docs/ARCHITECTURE.md`** — it is the binding contract for folder
layout, file ownership between parallel work packages, exact component/service APIs, styling
rules, and the native-interface boundary. See `CLAUDE.md` for the larger context and golden rules.
